import GameShell

extension GameIdentity {
    static let seep = GameIdentity(
        name: "SEEP",
        product: "seep",
        bundleID: "com.centaur-labs.seep",
        supportProductID: "seep.support"
    )
}
