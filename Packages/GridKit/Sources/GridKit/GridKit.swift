/// Grid primitives for square-cell puzzle games: layout, board view,
/// level store, seeded random.
/// Phase 1 of docs/shell-extraction.md: the package exists and links.
public enum GridKit {
    public static let version = "0.1.0"
}
