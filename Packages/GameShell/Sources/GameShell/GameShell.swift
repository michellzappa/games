/// Shared app shell for the game library: chrome, settings, audio,
/// telemetry, feedback, support purchase, Game Center, run store.
/// Phase 1 of docs/shell-extraction.md: the package exists and links.
public enum GameShell {
    public static let version = "0.1.0"
}
